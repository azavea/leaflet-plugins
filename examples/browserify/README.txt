This is an adaptation of the google example used to try using leaflet-plugins via require and browserify.

To run it, from the root directory, invoke the following commands
 * cd examples/browserify
 * npm install
 * npm install -g browserify
 * browserify google.js -o bundle.js
Then, open google.html in a browser.
